#include <tunables/global>

profile plant_care flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>
  #include <abstractions/ssl_certs>

  network inet stream,
  network inet6 stream,
  /usr/local/bin/python3.14 ix,
  /opt/venv/bin/python3 ix,
  /usr/bin/supervisord ix,
  /usr/sbin/nginx ix,
  /app/** r,
  /data/** rwk,
  /run/** rwk,
  /tmp/** rwk,
  /dev/null rw,
  /dev/urandom r,
}
